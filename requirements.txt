fastapi>=0.110
uvicorn[standard]>=0.29
scikit-learn>=1.3
pandas>=2.0
pydantic>=2.5
python-multipart>=0.0.9
Pillow>=10.0
pytesseract>=0.3.10

# Phase 2a runtime deps
httpx>=0.27
anthropic>=0.39
tldextract>=5.1
python-whois>=0.9.4

# Phase 3 — community scam database. Storage backend is chosen at runtime:
#   1. Postgres if DATABASE_URL is set (Railway Postgres injects this)
#   2. Firestore if FIRESTORE_PROJECT_ID is set
#   3. Local JSONL file otherwise (ephemeral on Railway — only good for dev)
google-cloud-firestore>=2.16
psycopg2-binary>=2.9

# Phase 2b runtime — AraBERT ONNX inference. Lightweight: tokenizers is
# pure Rust (no transformers/torch dep), onnxruntime is ~12 MB.
onnxruntime>=1.18
tokenizers>=0.19
numpy>=1.26

# camel-tools is optional at runtime (preprocess.py falls back to regex
# if it's missing). It's heavy (~200MB of MADAMIRA models on first use).
# Uncomment to enable on Railway, OR install locally for retraining only.
# camel-tools>=1.5
